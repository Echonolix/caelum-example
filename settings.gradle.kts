pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
    }

    plugins {
        id("org.jetbrains.kotlin.jvm") version "2.2.0"
    }
}

rootProject.name = "caelum-example"